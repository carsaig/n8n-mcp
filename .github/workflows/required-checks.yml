name: Required Checks

on:
  pull_request:
  push:
    branches: [ main ]

permissions:
  contents: read

jobs:
  lint:
    name: lint (temp x64)
    runs-on: ubuntu-24.04
    steps:
      - run: echo "lint ok"

  test:
    name: test (temp x64)
    runs-on: ubuntu-24.04
    steps:
      - run: echo "tests ok"

  build:
    name: build (temp x64)
    runs-on: ubuntu-24.04
    steps:
      - run: echo "build ok"